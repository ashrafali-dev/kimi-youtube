# nixpacks.toml - Install system dependencies on Railway
[phases.setup]
nixPkgs = ["ffmpeg", "python310", "pip"]

[phases.install]
cmds = [
  "pip install yt-dlp",
  "npm install"
]

[start]
cmd = "node server.js"
